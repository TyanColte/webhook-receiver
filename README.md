# webhook-receiver

**DEPRECATED.** This repo and `hooks.tyan.omegaos.us` are no longer used for stack deploys. Fully replaced (as of 2026-06-11) by `~/git/docker-stacks/.github/workflows/deploy.yml`, a single GitHub Actions workflow that detects changed stack folders via `git diff` and fires the relevant Portainer GitOps webhooks directly — no intermediate webhook-receiver container needed. See `docker-stacks` CLAUDE.md/`~/.claude/claudedetails.md` ("Stack Deploy Workflow") for the current mechanism.

Code below is left for reference only; not deployed.
